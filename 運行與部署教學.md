# 🚀 個人作品集網站 - 快速啟動與部署指南

> **適用專案**: React + TailwindCSS + Live2D 個人作品集  
> **最後更新**: 2025-12-25

---

## 📋 目錄

1. [環境需求](#環境需求)
2. [快速開始](#快速開始)
3. [開發指令](#開發指令)
4. [常見問題與解決方案](#常見問題與解決方案)
5. [部署到 GitHub Pages](#部署到-github-pages)
6. [自訂內容](#自訂內容)
7. [疑難排解](#疑難排解)

---

## 🔧 環境需求

### 必要軟體

- **Node.js**: 18.18.0 或更高版本
- **npm**: 9.0.0 或更高版本
- **Git**: 最新版本

### 檢查版本

```bash
node -v    # 應顯示 v18.x.x 或更高
npm -v     # 應顯示 9.x.x 或更高
git --version
```

---

## ⚡ 快速開始

### 步驟 1: 安裝依賴

```bash
# 進入專案目錄
cd fa_der-of-U2077.github.io

# 安裝所有必要套件
npm install
```

**遇到網路問題？** 使用淘寶鏡像源：
```bash
npm install --registry=https://registry.npmmirror.com
```

### 步驟 2: 啟動開發伺服器

```bash
npm run dev
```

**成功訊息**:
```
VITE v5.4.21  ready in 509 ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

### 步驟 3: 在瀏覽器中查看

開啟瀏覽器，訪問 `http://localhost:5173/`

---

## 💻 開發指令

### 常用指令表

| 指令 | 說明 | 使用時機 |
|------|------|----------|
| `npm install` | 安裝所有依賴套件 | 首次使用或更新套件後 |
| `npm run dev` | 啟動開發伺服器 | 本地開發時 |
| `npm run build` | 建置生產版本 | 部署前測試 |
| `npm run preview` | 預覽建置結果 | 檢查生產版本 |
| `npm run deploy` | 部署到 GitHub Pages | 發佈網站 |

### 詳細說明

#### 1. 開發模式 (Development)

```bash
npm run dev
```

- ✅ 自動熱重載 (Hot Module Replacement)
- ✅ 即時預覽修改
- ✅ 快速反饋開發結果
- 📍 預設端口: `http://localhost:5173/`

**停止伺服器**: 按 `Ctrl + C`

---

#### 2. 建置專案 (Build)

```bash
npm run build
```

**功能**:
- 編譯 React 代碼
- 優化 CSS (TailwindCSS)
- 壓縮 JavaScript
- 產生生產環境檔案到 `dist/` 資料夾

**輸出範例**:
```
✓ 45 modules transformed.
dist/index.html                   1.89 kB │ gzip:  0.87 kB
dist/assets/index-DXQ2e3-V.css   18.50 kB │ gzip:  3.91 kB
dist/assets/index-BkzQLVUZ.js   183.30 kB │ gzip: 60.29 kB
✓ built in 2.64s
```

---

#### 3. 預覽建置結果

```bash
npm run preview
```

在本地預覽生產版本，確保一切正常後再部署。

---

## 🚢 部署到 GitHub Pages

### 方法 1: 使用 gh-pages 套件（推薦）

#### 前置準備

1. **確認 Git Repository 已初始化**

```bash
# 檢查 Git 狀態
git status

# 如果尚未初始化
git init
git add .
git commit -m "Initial commit"
```

2. **設置 GitHub Token（重要！）**

由於 GitHub 已不支援密碼驗證，需使用 Personal Access Token：

##### 步驟 A: 創建 GitHub Token

1. 登入 GitHub
2. 前往 **Settings** → **Developer settings** → **Personal access tokens** → **Tokens (classic)**
3. 點擊 **Generate new token** → **Generate new token (classic)**
4. 設置 Token 資訊:
   - **Note**: `GitHub Pages Deployment`
   - **Expiration**: 選擇適當期限
   - **Select scopes**: 勾選 `repo` (完整 repository 權限)
5. 點擊 **Generate token**
6. **立即複製 Token**（離開頁面後無法再次查看！）

##### 步驟 B: 配置 Git 認證

**Windows 系統**:

```bash
# 使用 Git Credential Manager
git config --global credential.helper manager-core

# 或直接在 URL 中包含 Token（不推薦，僅測試用）
git remote set-url origin https://<YOUR_TOKEN>@github.com/fa_der-of-U2077/fa_der-of-U2077.github.io.git
```

**macOS/Linux 系統**:

```bash
# 使用 osxkeychain (macOS)
git config --global credential.helper osxkeychain

# 使用 cache (Linux)
git config --global credential.helper cache
```

##### 步驟 C: 執行部署

```bash
npm run deploy
```

**成功訊息**:
```
Published
```

---

### 方法 2: GitHub Actions（持續自動部署）

#### 優點
- ✅ 推送到 main 分支自動部署
- ✅ 無需本地配置 Token
- ✅ 適合團隊協作

#### 設置步驟

1. **確認 `.github/workflows/deploy.yml` 已存在**（專案已包含）

2. **啟用 GitHub Pages**
   - 前往 Repository **Settings**
   - 點選 **Pages**
   - **Source** 選擇 **GitHub Actions**

3. **推送代碼**
```bash
git add .
git commit -m "Enable GitHub Actions deployment"
git push origin main
```

4. **檢查部署狀態**
   - 前往 Repository 的 **Actions** 分頁
   - 查看 workflow 執行狀態
   - 成功後網站會自動上線

---

## ✏️ 自訂內容

### 更新個人資訊

編輯 `src/components/sections/ProfileSection.jsx`:

```jsx
// 修改個人資訊
<h1 className="text-5xl font-bold text-gray-900 mb-2">
  你的名字
</h1>

// 更新連結
<a href="https://github.com/YOUR_USERNAME" className="...">
  <FaGithub size={20} />
  <span>GitHub</span>
</a>
```

---

### 新增研究專案

編輯 `src/components/sections/ProjectsSection.jsx`:

```jsx
const projects = [
  {
    title: '🔹 你的專案標題',
    items: [
      '專案描述 1',
      '專案描述 2',
      '專案描述 3'
    ],
    techs: [
      '技術1',
      '技術2',
      '技術3'
    ]
  },
  // ... 繼續新增
];
```

---

### 新增論文發表

編輯 `src/components/sections/PublicationsSection.jsx`:

```jsx
const publications = [
  {
    title: '你的論文標題',
    venue: '會議或期刊名稱',
    year: '2025'
  },
  // ... 繼續新增
];
```

---

### 調整技能評分

編輯 `src/components/sections/SkillsSection.jsx`:

```jsx
skills: [
  { name: 'Python', icon: FaPython, level: 95 },  // 修改 level 值 (0-100)
  { name: '新技能', level: 80 }  // 新增技能
]
```

---

## 🐛 疑難排解

### 問題 1: 建置錯誤 - 圖標找不到

**錯誤訊息**:
```
"FaXxx" is not exported by "node_modules/react-icons/fa/index.mjs"
```

**解決方案**:

1. 檢查圖標是否存在於正確的套件中
2. 查閱 [React Icons 文件](https://react-icons.github.io/react-icons/)
3. 使用替代圖標

**常見替換**:
- `FaGoogleScholar` → 使用 `SiGooglescholar` (從 `react-icons/si`)
- `SiCuda` → 使用 `FaMicrochip` (從 `react-icons/fa`)

---

### 問題 2: npm install 網路逾時

**錯誤訊息**:
```
npm ERR! network Socket timeout
```

**解決方案**:

```bash
# 使用淘寶鏡像源
npm install --registry=https://registry.npmmirror.com

# 或永久設置
npm config set registry https://registry.npmmirror.com
```

---

### 問題 3: GitHub 部署驗證失敗

**錯誤訊息**:
```
remote: Invalid username or token. Password authentication is not supported
```

**解決方案**:

參考 [部署到 GitHub Pages](#部署到-github-pages) 中的 Token 設置步驟。

**快速檢查清單**:
- [ ] 已創建 GitHub Personal Access Token
- [ ] Token 包含 `repo` 權限
- [ ] Git 已配置 credential helper
- [ ] Remote URL 正確

---

### 問題 4: 端口 5173 被佔用

**錯誤訊息**:
```
Port 5173 is in use
```

**解決方案 A**: 停止佔用端口的程序

```bash
# Windows
netstat -ano | findstr :5173
taskkill /PID <PID> /F

# macOS/Linux
lsof -i :5173
kill -9 <PID>
```

**解決方案 B**: 使用其他端口

修改 `vite.config.js`:
```js
export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000  // 改為其他端口
  }
})
```

---

### 問題 5: Live2D 角色沒有顯示

**檢查清單**:

1. **檢查 Console 是否有錯誤**
   - 按 `F12` 開啟開發者工具
   - 查看 Console 分頁

2. **確認 CDN 連結正常**
   - 檢查 `index.html` 中的 Live2D script 標籤
   - 測試 CDN 連結是否可訪問

3. **清除瀏覽器快取**
   - Chrome: `Ctrl + Shift + Delete`
   - 勾選「快取的圖片和檔案」
   - 點擊「清除資料」

4. **檢查 CSS z-index**

確認 `src/index.css` 包含:
```css
#live2d-widget {
  position: fixed !important;
  bottom: 0 !important;
  left: 0 !important;
  z-index: 999 !important;
}
```

---

### 問題 6: 修改後沒有更新

**解決方案**:

```bash
# 停止開發伺服器 (Ctrl + C)
# 清除快取
rm -rf node_modules/.vite

# 重新啟動
npm run dev
```

---

## 📚 相關資源

### 官方文件
- [React 文件](https://react.dev/)
- [Vite 文件](https://vitejs.dev/)
- [TailwindCSS 文件](https://tailwindcss.com/)
- [React Icons](https://react-icons.github.io/react-icons/)
- [Live2D Widget](https://github.com/stevenjoezhang/live2d-widget)

### GitHub Pages
- [GitHub Pages 文件](https://docs.github.com/en/pages)
- [gh-pages 套件](https://github.com/tschaub/gh-pages)

---

## 📝 開發工作流程建議

### 日常開發

```bash
# 1. 啟動開發伺服器
npm run dev

# 2. 編輯代碼 (自動重載)

# 3. 本地測試完成後，提交變更
git add .
git commit -m "Update: 描述你的修改"

# 4. 建置測試
npm run build

# 5. 部署
npm run deploy
```

---

### 版本管理建議

```bash
# 創建功能分支
git checkout -b feature/new-section

# 開發完成後合併
git checkout main
git merge feature/new-section

# 推送到遠端
git push origin main
```

---

## ✅ 部署檢查清單

部署前請確認：

- [ ] 所有修改已提交到 Git
- [ ] `npm run build` 建置成功
- [ ] 本地預覽正常 (`npm run preview`)
- [ ] GitHub Token 已正確配置
- [ ] 個人資訊已更新（姓名、連結等）
- [ ] Live2D 角色正常顯示
- [ ] 響應式設計在各種螢幕尺寸下正常
- [ ] 所有連結可以正常點擊

---

## 🎉 恭喜完成！

您的個人作品集網站已成功建立並運行！

**網站地址**: `https://fa_der-of-U2077.github.io/`

**下一步**:
1. 定期更新研究專案和論文發表
2. 添加更多個人特色內容
3. 優化 SEO 和社交媒體分享
4. 考慮加入 Google Analytics 追蹤訪客

---

## 📞 需要協助？

- 檢查 [常見問題](#疑難排解) 章節
- 查閱官方文件
- 在 GitHub 上開 Issue

**專案維護者**: fa_der-of-U2077  
**最後更新**: 2025-12-25

---

**Happy Coding! 🚀**
